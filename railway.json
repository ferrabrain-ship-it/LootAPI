{
  "$schema": "https://railway.com/railway.schema.json",
  "builder": "RAILPACK",
  "deploy": {
    "startCommand": "sh -lc 'PLAYWRIGHT_BROWSERS_PATH=/app/ms-playwright npx playwright install chromium chromium-headless-shell && npm run start'",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
